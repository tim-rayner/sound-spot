export default defineEventHandler((event) => {
  if (event.method === "PUT") {
    //update a blog by id
    // return the blog;
  }
  if (event.method === "DELETE") {
    // delete a blog by id
    // return success;
  }
});
